# Minimal override on top of config/default.yaml for river corridor clipping.

clipping:
  enabled: true
  boundary_file: data/raw/river_polygon_25833.geojson

logging:
  file: logs/river_clipped_processing.log
