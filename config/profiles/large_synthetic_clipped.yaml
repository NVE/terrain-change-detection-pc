# Minimal override preset for clipped large synthetic runs.

paths:
  base_dir: data/large_synthetic

clipping:
  enabled: true
  boundary_file: data/large_synthetic/clip_areas.geojson

alignment:
  max_correspondence_distance: 2.0
  subsample_size: 100000
  coarse:
    method: phase
    voxel_size: 3.0
    phase_grid_cell: 3.0

detection:
  dod:
    enabled: true
    cell_size: 2.0
    export_raster: true
  c2c:
    enabled: true
    max_points: 10000
    export_pc: true
    export_raster: true
  m3c2:
    use_autotune: true
    core_points_percent: 10.0
    autotune:
      target_neighbors: 20
      max_depth_factor: 0.6
      min_radius: 2.0
      max_radius: 50.0
    fixed:
      radius: null
      normal_scale: null
      depth_factor: null

logging:
  file: logs/large_synthetic_clipped_processing.log

outofcore:
  enabled: true
  halo_m: 30.0
  chunk_points: 2000000

parallel:
  enabled: true
